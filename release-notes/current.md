## 本次更新

- 图片任务与裁切资源卡片左侧新增动态层级序号，拖拽重排后立即重新编号。
- 图片任务支持根层、组内与跨组拖拽排序，顺序会随工作区保存和恢复。
- 裁切资源编组升级为多级树结构，普通文件夹、来源目录和已有编组均可拖入编组。
- 裁切资源拖拽支持卡片中部收纳、上下边缘同级插入，并阻止编组移入自身或后代。
- 编组文件夹右键菜单新增重命名，支持名称校验与同名自动避让。
- 兼容旧版工作区快照，并完成控制器、持久化和真实鼠标拖拽组件回归。

> Windows 安装包当前未使用公共代码签名证书，部分设备可能显示 SmartScreen 提示。
